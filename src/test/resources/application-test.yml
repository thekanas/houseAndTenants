spring:
  datasource:
    username: test
    password: test


  jpa:
    properties.hibernate:
        hbm2ddl.auto: validate

  liquibase:
    changelog: db.changelog/db.changelog-master-test.yaml
    enabled: true